#include "Scene/SceneLight.hpp"

namespace wallpaper
{

// Stubbed; body filled in by the next commit.  Returning false here means
// in-progress builds compile + link + ship dark — safest interim state.
bool SceneLight::castsVolumetrics() const { return false; }

} // namespace wallpaper
